# OpenMemory Dashboard - Local Development Environment Variables

# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8080

# ============================================
# Authentication Configuration
# ============================================
# At least one authentication method must be enabled (set to 'true')
ENABLE_PASSWORD_AUTH=true
ENABLE_GITHUB_SSO=false

# --------------------------------------------
# Password Authentication
# --------------------------------------------
# Required if ENABLE_PASSWORD_AUTH=true
# Generate a secure password: openssl rand -base64 32
DASHBOARD_PASSWORD=your-secure-password-here

# --------------------------------------------
# GitHub OAuth (Local Development)
# --------------------------------------------
# Required if ENABLE_GITHUB_SSO=true
# 
# Setup Instructions:
# 1. Go to: https://github.com/settings/developers
# 2. Click "New OAuth App"
# 3. Fill in:
#    - Application name: OpenMemory Dashboard (Local)
#    - Homepage URL: http://localhost:3000
#    - Authorization callback URL: http://localhost:3000/api/auth/callback/github
# 4. Click "Register application"
# 5. Copy the Client ID below
# 6. Click "Generate a new client secret" and copy it below
# 7. Set your GitHub username in ALLOWED_GITHUB_USERNAME

AUTH_GITHUB_ID=your-github-client-id
AUTH_GITHUB_SECRET=your-github-client-secret
ALLOWED_GITHUB_USERNAME=your-github-username

# --------------------------------------------
# NextAuth Configuration
# --------------------------------------------
# Required for all authentication methods
# Generate a random secret: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-here

# Local development URL
NEXTAUTH_URL=http://localhost:3000

# ============================================
# Testing Different Auth Configurations
# ============================================
# 
# Password Only (simplest for local dev):
#   ENABLE_PASSWORD_AUTH=true
#   ENABLE_GITHUB_SSO=false
#
# GitHub Only (most convenient):
#   ENABLE_PASSWORD_AUTH=false
#   ENABLE_GITHUB_SSO=true
#
# Both Methods (maximum flexibility):
#   ENABLE_PASSWORD_AUTH=true
#   ENABLE_GITHUB_SSO=true
